# 
# GitHub Actions workflow.
#
# m1 pipeline
#

name: m1 build-release

on:
  push:
    branches: 
      - '**'
    paths:
      - 'm1/**'
      - .github/workflows/m1*
      - .github/workflows/shared/build-release.yaml
  workflow_dispatch:

jobs:

  Build:
    uses: ./.github/workflows/shared/build-release.yaml
    with:
      module: m1
