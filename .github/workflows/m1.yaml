# 
# GitHub Actions workflow.
#
# m1 pipeline
#

name: m1 build-release

on:
  push:
    branches: 
      - '**'
    paths:
      - 'm1/**'
      - .github/workflows/m1*
      - .github/workflowsbuild-release.yaml
  workflow_dispatch:

jobs:

  Build:
    uses: ./.github/workflowsbuild-release.yaml
    with:
      module: m1
